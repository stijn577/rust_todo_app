[build]
target = "index.html"
dist = "../dist"

# [[proxy]]
# backend = "http://[::1]:8080/api/"

[[hooks]]
stage = "pre_build"
command = "nu"
command_arguments = ["-c", "tailwindcss -i ./css/input.css -o ./css/output.css"]
